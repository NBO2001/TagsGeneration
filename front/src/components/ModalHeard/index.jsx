import React from 'react'
import { Heard } from './style'

const ModalHeard = ({children}) => {
    return (
        <Heard>
            {children}
        </Heard>
    )
}

export default ModalHeard
